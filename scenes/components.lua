local forms = require("forms")
local stateManager = require("stateManager")
local components = {}
	
	components['authButton'] = {w=1, h=1, caption='', onClick=function() return end}

return components